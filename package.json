{
  "name": "www",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "purgecss": "^1.4.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-https-redirect": "^1.1.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "tailwind && NODE_PATH=src react-scripts start",
    "build": "tailwind && node ./purge-tailwindcss.js && NODE_PATH=src GENERATE_SOURCEMAP=false react-scripts --openssl-legacy-provider build",
    "start-o": "PORT=9090 react-scripts start",
    "build-o": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^1.9.6"
  }
}
